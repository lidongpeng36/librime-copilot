#!/usr/bin/env python3
"""Entry point for the corpus and evaluation harness."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from rime_corpus.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
